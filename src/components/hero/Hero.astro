---
import { Image } from 'astro:assets';
import Button from '../Button.astro';
import imageHero from '../../assets/screen-mockups.svg';
import iconComunities from '../../assets/icon-communities.svg';
import iconMessage from '../../assets/icon-messages.svg';
import CardValue from './CardValue.astro';
---

<div class="container">
    <section>
        <h1>Build The Community Your Fans Will Love</h1>
        <p class="heading-p">
            Huddle re-imagines the way we build communities. You have a voice,
            but so does your audience. Create connections with your users as you
            engage in genuine discussion.
        </p>
        <Button />
    </section>
    <div class="wrap-image">
        <Image src={imageHero} alt="images of two devices" />
    </div>
    <aside>
        <CardValue
            imgSrc={iconComunities}
            imgAlt="icon communities"
            text="1.4k+"
            description="Communities Formed"
        />
        <CardValue
            imgSrc={iconMessage}
            imgAlt="icon message"
            text=" 2.7m+"
            description="Messages Sent"
        />
    </aside>
</div>

<style>
    section {
        margin-block: 5rem 6rem;
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1.5rem;
    }

    h1 {
        font-size: clamp(1.6rem, 5vw, 3rem);
    }

    .heading-p {
        font-size: clamp(15px, 3vw, 18px);
        color: var(--very-dark-cyan);
        margin-bottom: 1.5rem;
    }

    img {
        height: 100%;
    }

    aside {
        margin-top: 7rem;
        display: grid;
        justify-content: center;

        gap: 7rem;
    }
</style>
